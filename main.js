
import axios, {isCancel, AxiosError} from 'axios';
const { TheCatAPI } = require("@thatapicompany/thecatapi");

//const Job={
    //constructor(Role,Level,Weapon,Health,SpecialMove){
        this.Role= Role;
        this.Level=Level;
        this.Weapon=Weapon;
        this.Health=Health;
        this.SpecialMove=SpecialMove;
    },
    //takeDamage(){
        console.log(this.Health, 'You took 50 damage!');
        SpecialMove()
        console.log (this.SpecialMove,'Lets GOOOO!');

    
    }};

//const War= new Job('Tank',90, 'Conjurers Staff', 500, 'Fellcleave')
//const Whm= new Job('Healer',90, 'Conjurers Staff', 250, 'Benediction')
//const Drg= new Job('DPS',90,'Gaebolg Lance',300,'Jump')//

    
//Damage Mitigation When Hit
const DmgMTG= DmgMTG((resolve, reject) => {
    setTimeout(() => {
        resolve("Dodge,Block! or something!");{
            console.log(this.SpecialMove,'That was close!!')
            
            reject('Ouch, I didnt mske it in time');{
                console.log('Ouch, I didnt make it in time')
            }
      }

    }, 1000);
});

takeDamage

.then((DmgMTG) => takeDamage + DmgMTG,'Again?')
    .then((DmgMTG) => takeDamage + DmgMTG, 'Third time!')
.catch((err) => {
        console.error(err);
    })

    function info(){
        const getInfo= ()=>{
            axios.get('')
        .then(res => {
            console.log(res)

}) .catch(err => {
    console.log(err)
})
        }

;
    }